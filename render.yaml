services:
  - type: web
    name: tiza-film-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      BOT_TOKEN:
        fromSecret: BOT_TOKEN
